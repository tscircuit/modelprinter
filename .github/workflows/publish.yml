name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: npm-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.2
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org/
          package-manager-cache: false
      - run: bun install
      - run: bun test
      - run: bun run typecheck
      - run: bun run format:check
      - run: bun run build
      - run: npm pack --dry-run
      - run: npm publish --access public --provenance
        env:
          # Optional token for the initial publish; subsequent releases may use
          # npm trusted publishing with this workflow's OIDC identity.
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
